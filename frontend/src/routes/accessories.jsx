import { createFileRoute } from "@tanstack/react-router";
import { CategoryPage } from "@/components/CategoryPage";import { jsx as _jsx } from "react/jsx-runtime";
export const Route = createFileRoute("/accessories")({ component: () => /*#__PURE__*/_jsx(CategoryPage, { slug: "accessories", title: "Accessories", subtitle: "Bags, sunglasses, and the finishing touches that complete every fit." }) });