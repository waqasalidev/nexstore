import { createFileRoute } from "@tanstack/react-router";
import { CategoryPage } from "@/components/CategoryPage";import { jsx as _jsx } from "react/jsx-runtime";
export const Route = createFileRoute("/watches")({ component: () => /*#__PURE__*/_jsx(CategoryPage, { slug: "watches", title: "Watches", subtitle: "Heritage timepieces and next-generation smartwatches. Rotate, zoom, inspect." }) });